version: "3.8"

services:
  web:
    build: .
    image: bookstore-web:latest
    environment:
      - PORT=4000
      - API_URL=${API_URL:-http://localhost:8080/api/v1}
    ports:
      - "4000:4000"
    restart: unless-stopped
